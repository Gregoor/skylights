{
  "lexicon": 1,
  "id": "my.skylights.list",
  "defs": {
    "main": {
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "items": { "type": "array", "items": { "$ref": "my.skylights.item" } }
      },
      "required": ["title", "items"]
    }
  }
}
