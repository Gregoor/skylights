{
  "lexicon": 1,
  "id": "love.media.list",
  "defs": {
    "main": {
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "items": { "type": "array", "items": { "$ref": "love.media.item" } }
      },
      "required": ["title", "items"]
    }
  }
}
